{
  "name": "gamechat-ai-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@sentry/nextjs": "^9.30.0",
    "@sentry/react": "^9.30.0",
    "@sentry/vite-plugin": "^3.5.0"
  }
}
