# =============================================================================
# GameChat AI 環境変数テンプレート
# =============================================================================
# ⚠️  SECURITY NOTICE: このファイルは環境変数のテンプレートです
# 実際のAPIキーやシークレットは含まれていません
# gitguardian:ignore

# 使用方法:
# 【開発環境】
# 1. backend/.env.production.templateをbackend/.envとしてコピー
# 2. frontend/.env.local.templateをfrontend/.env.localとしてコピー
# 3. 各ファイルに実際のAPIキーと設定値を入力
# 
# 【本番環境】
# 1. backend/.env.production.templateとfrontend/.env.production.templateを使用
# 2. 環境変数やDocker Secrets等で機密情報を管理

# =============================================================================
# バックエンド設定例 (開発環境: backend/.env)
# =============================================================================

# 環境設定
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=DEBUG

# API・セキュリティ設定
API_V1_STR=/api/v1
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# レート制限設定
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600
REDIS_URL=redis://localhost:6379/0

# OpenAI API設定 (必須)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Upstash Vector Database設定 (必須)
UPSTASH_VECTOR_REST_URL=https://xxxxxxxxxx-xxxxxxxxxx-vector.upstash.io
UPSTASH_VECTOR_REST_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# reCAPTCHA設定
RECAPTCHA_SECRET=6Lc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RECAPTCHA_SITE=6Lc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RECAPTCHA_SECRET_TEST=6Lc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# CORS設定（開発環境）
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# データファイルパス設定（オプション）
# 指定しない場合はプロジェクト相対パスが自動設定されます
# DATA_FILE_PATH=/path/to/your/data.json
# CONVERTED_DATA_FILE_PATH=/path/to/your/convert_data.json
# EMBEDDING_FILE_PATH=/path/to/your/embedding_list.jsonl
# QUERY_DATA_FILE_PATH=/path/to/your/query_data.json

# データベース設定（SQLite/PostgreSQL等）
# DATABASE_URL=sqlite:///./gamechat.db

# =============================================================================
# フロントエンド設定例 (開発環境: frontend/.env.local)
# =============================================================================

# API URL設定
NEXT_PUBLIC_API_URL=http://localhost:8000

# reCAPTCHA設定
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=6Lc-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# アプリケーション設定
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
NEXT_PUBLIC_ENVIRONMENT=development

# =============================================================================
# 本番環境設定のポイント
# =============================================================================
# 本番環境では以下の設定を変更してください：

# セキュリティ設定
# - SECRET_KEY: 本番用の強力なランダムキーを生成
# - DEBUG=false, LOG_LEVEL=WARNING
# - CORS_ORIGINS: 本番ドメインのみに制限
# - RATE_LIMIT_ENABLED=true で適切な制限値を設定

# 認証・API設定
# - 本番用のOpenAI APIキー
# - 本番用のUpstash設定
# - 本番用のreCAPTCHA設定

# インフラ設定
# - Redis URL（本番Redis環境）
# - DATABASE_URL（本番データベース）
# - ログファイルパス等

# 詳細は各テンプレートファイルを参照:
# - backend/.env.production.template
# - frontend/.env.production.template
# - frontend/.env.local.template