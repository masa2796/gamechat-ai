## GameChat AI Backend - .env.ci (MVP)
# CI でのスモークテスト/ビルドに必要な最小セット

BACKEND_ENVIRONMENT=test
BACKEND_DEBUG=true
BACKEND_LOG_LEVEL=INFO

# OpenAI: CI では未設定でも擬似Embeddingで動作
BACKEND_OPENAI_API_KEY=sk-test_openai_key

# Upstash Vector: ダミー値（ライブラリなし/未設定でもフォールバック）
UPSTASH_VECTOR_REST_URL=https://example-vector-db.upstash.io
UPSTASH_VECTOR_REST_TOKEN=test-token

# CORS
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# モック有効化
BACKEND_TESTING=true
BACKEND_MOCK_EXTERNAL_SERVICES=true
