## GameChat AI Backend - .env.example (MVP)
# このファイルをコピーして backend/.env として使用してください。
# MVPでは最低限のキーのみ使用します。

# 環境: development | test | production
BACKEND_ENVIRONMENT=development
BACKEND_DEBUG=true
BACKEND_LOG_LEVEL=INFO

# OpenAIが未設定でも動作（擬似Embeddingにフォールバック）
BACKEND_OPENAI_API_KEY=

# Upstash Vector（未設定でも動作。未設定時はダミータイトルにフォールバック）
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# CORS（ローカル開発用）
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# テスト・モック制御（ローカル開発では通常 false / true 推奨）
BACKEND_TESTING=false
BACKEND_MOCK_EXTERNAL_SERVICES=true
