## GameChat AI Backend - .env.example (MVP 最小構成)
# backend/.env としてコピーして利用してください。
# 未設定でも起動できる設計。必要に応じて値を入れてください。

# 基本ランタイム設定
BACKEND_ENVIRONMENT=development        # development | test | production
BACKEND_DEBUG=true                     # 詳細ログを有効化
BACKEND_LOG_LEVEL=INFO                 # INFO 推奨 (DEBUG は騒がしい)

# OpenAI (未設定OK: 擬似Embedding + スタブ回答)
BACKEND_OPENAI_API_KEY=

# Upstash Vector (未設定OK: ダミータイトル生成フォールバック)
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# フロントエンド (ローカル開発CORS許可)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# フォールバック/モック制御
BACKEND_TESTING=false                  # 通常は false
BACKEND_MOCK_EXTERNAL_SERVICES=true    # 外部APIを基本モック運用（MVP段階推奨）

# 追加で本番切替時に検討する項目 (MVP外)
# BACKEND_SECRET_KEY=change-me
# GCS_BUCKET_NAME=gamechat-ai-data

# (任意) Redis / DB 等は MVP では未使用のため記載省略

# ---- 説明 ----
# 1. OpenAI / Upstash 未設定でも /chat はフォールバックし 200 を返します
# 2. フロントは NEXT_PUBLIC_MVP_MODE=true で /chat を直接叩く構成
# 3. 不要な高度機能 (認証 / ハイブリッド検索 / モニタリング) の変数は除外

